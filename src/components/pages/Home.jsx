import Layout from '../layout/Layout';
import MainButton from '../MainButton';

const Home = () => {
  return (
    <Layout>
      <MainButton />
      <MainButton />
      <div>메인 페이지 (삭제예정)</div>
    </Layout>
  );
};

export default Home;
