import Layout from '../layout/Layout';
import WorryList from '../WorryList';

const ListPage = () => {
  return (
    <Layout>
      <WorryList />
      <div>리스트페이지 (삭제예정)</div>
    </Layout>
  );
};

export default ListPage;
